// Constante "configurable" del modal de promociones — mismo patrón que
// lib/booking/config.ts (editable acá, sin UI de configuración).

/** Días que dura la cookie de "ya viste esta promo" antes de volver a mostrarla. */
export const PROMO_DISMISS_COOKIE_DAYS = 7;
